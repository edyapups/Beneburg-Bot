# Local release settings. Database secrets belong only in the server env file.
RELEASE_HOST=deploy@example.com
RELEASE_DIR=/opt/beneburg
RELEASE_ENV_FILE=.env.local
RELEASE_IMAGE_REPO=beneburg
RELEASE_PLATFORM=linux/amd64
