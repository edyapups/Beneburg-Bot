# Copy the values into your shell profile or a local untracked file before release.
RELEASE_HOST=deploy@example.com
RELEASE_DIR=/opt/beneburg
RELEASE_ENV_FILE=.env.local
RELEASE_IMAGE_REPO=beneburg
# RELEASE_SQLITE_DB=/absolute/path/to/beneburg.db
